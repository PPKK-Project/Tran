## 🌟 프로젝트 API 명세서 (정리본)

## 0. 사용자 및 인증 관리 (User & Auth)

| 목표 | 메소드 | 엔드포인트 | 설명 |
| :--- | :--- | :--- | :--- |
<!-- | **회원가입** | `POST` | `/signup` | 신규 사용자 계정을 생성합니다. | -->
<!-- | **로그인** | `POST` | `/login` | 사용자 인증 후 JWT 토큰을 발급합니다. | -->
<!-- | **내 정보 수정** | `PATCH` | `/users` | 사용자 정보를 수정합니다. | -->

## 1. TRAVEL (여행 정보 관리)

| 엔드포인트 | HTTP 메서드 | 설명 |
| :--- | :--- | :--- |
| `/travels` | **`POST`** | 새로운 여행 계획 생성 |
| `/travels` | **`GET`** | 사용자가 접근 가능한 모든 여행 목록 조회 |
| `/travels/{travelId}` | **`GET`** | 특정 여행의 상세 정보 조회 |
| `/travels/{travelId}` | **`PUT`** | 특정 여행의 기본 정보 수정 |
| `/travels/{travelId}` | **`DELETE`** | 특정 여행 삭제 (OWNER만 가능) |

---

## 2. TRAVEL\_PLAN (여행 일정 관리)

| 엔드포인트 | HTTP 메서드 | 설명 |
| :--- | :--- | :--- |
| `/travels/{travelId}/plans` | **`POST`** | 여행 일정에 새로운 장소 추가 |
| `/travels/{travelId}/plans` | **`GET`** | 특정 여행의 전체 일정 목록 조회 |
| `/travels/{travelId}/plans/{planId}` | **`PUT`** | 특정 일정 항목의 세부 정보(순서, 일차, 메모) 수정 |
| `/travels/{travelId}/plans/{planId}` | **`DELETE`** | 특정 일정 항목 삭제 |

---

## 3. PLACE & Detail Subtypes (장소 정보 관리)

| 엔드포인트 | HTTP 메서드 | 설명 |
| :--- | :--- | :--- |
| `/places/{placeId}` | **`GET`** | 특정 장소의 상세 정보 조회 |
---

## 4. TRAVEL\_PERMISSION (여행 공유 및 권한 관리)

| 엔드포인트 | HTTP 메서드 | 설명 |
| :--- | :--- | :--- |
| `/travels/{travelId}/permissions` | **`POST`** | 여행에 다른 사용자 초대 및 권한 부여 |
| `/travels/{travelId}/permissions` | **`GET`** | 해당 여행의 모든 공유 권한 목록 조회 |
| `/travels/{travelId}/permissions/{permissionId}` | **`PUT`** | 특정 사용자 권한(역할) 수정 (OWNER만 가능) |
| `/travels/{travelId}/permissions/{permissionId}` | **`DELETE`** | 특정 사용자 권한 삭제 (공유 취소) |

